{
  "name": "transkript",
  "version": "0.1.5",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "release": "npm run build && npm run lint && npm version patch && git push --follow-tags",
    "release:minor": "npm run build && npm run lint && npm version minor && git push --follow-tags",
    "release:major": "npm run build && npm run lint && npm version major && git push --follow-tags",
    "prerelease": "npm run build && npm run lint"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next": "15.3.5"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.3.5",
    "@eslint/eslintrc": "^3"
  }
}
